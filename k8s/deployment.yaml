apiVersion: apps/v1
kind: Deployment
metadata:
  name: api.ogeraldoluiz
  namespace: development
  labels:
    app: api.ogeraldoluiz
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api.ogeraldoluiz
  template:
    metadata:
      labels:
        app: api.ogeraldoluiz
    spec:
      containers:
        - name: api.ogeraldoluiz
          image: geraldobl58/api.ogeraldoluiz:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: development
          resources:
            limits:
              cpu: 300m
              memory: 256Mi
            requests:
              cpu: 150m
              memory: 128Mi